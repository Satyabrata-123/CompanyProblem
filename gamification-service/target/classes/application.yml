server:
  port: 8084

spring:
  application:
    name: gamification-service
  datasource:
    url: jdbc:mysql://localhost:3306/GamificationService
    username: root
    password: sql24
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
